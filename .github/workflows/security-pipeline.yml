name: Pipeline de Segurança DevSecOps Unificado

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-scan:
    name: 1. Build, Testes Unitários e SCA/SAST
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      actions: read
      contents: read

    steps:
      - uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Instalar dependências Python
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-cov PyJWT

      - name: Criar banco de dados SQLite
        run: python -c "from app import init_db; init_db()"

      - name: Rodar testes com pytest
        run: |
          mkdir -p reports
          pytest tests/ --maxfail=1 --disable-warnings -v | tee reports/pytest.log
        continue-on-error: false

      - name: Upload dos logs de teste
        uses: actions/upload-artifact@v4
        with:
          name: pytest-logs
          path: reports/pytest.log

      - name: Instalar Snyk CLI
        run: npm install -g snyk

      - name: Executar SCA com Snyk
        run: snyk test --severity-threshold=high --fail-on=all --skip-unresolved
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}

      - name: Inicializar CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: python

      - name: Executar análise CodeQL
        uses: github/codeql-action/analyze@v3
        env:
          CODEQL_FAIL_ON_ALERT: "true"

  dynamic-analysis:
    name: 2. Análise Dinâmica (DAST)
    runs-on: ubuntu-latest
    needs: build-and-scan

    # --- CONFIGURAÇÃO DE SERVIÇOS DO DOCKER ---
    services:
      # Serviço da Aplicação Flask
      app:
        # A imagem será construída no passo "Build Docker image do Flask"
        image: my-flask-app
        options: >-
          --name flask-app
          # Verifica se a aplicação está rodando na porta 5000 interna
          --health-cmd "curl -f http://localhost:5000 || exit 1" 
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5000:5000 # Mapeia a porta para o host (para o health-check no step)

      # Serviço do OWASP ZAP (apenas para colocá-lo na mesma rede)
      zap:
        image: ghcr.io/zaproxy/zaproxy:stable
        # Define um hostname simples para a comunicação interna, embora o scan será feito no serviço 'app'
        options: --hostname zap-scanner
        ports:
          - 8080:8080 # Opcional, para debug

    steps:
      - uses: actions/checkout@v3

      - name: Criar Dockerfile temporário
        run: |
          cat > Dockerfile <<'EOF'
          FROM python:3.10-slim
          WORKDIR /app
          COPY . .
          RUN pip install --no-cache-dir -r requirements.txt
          EXPOSE 5000
          CMD ["python", "app.py"]
          EOF

      - name: Build Docker image do Flask
        # Este passo constrói a imagem necessária para o serviço 'app'
        run: docker build -t my-flask-app .

      - name: Aguardar o Flask iniciar e passar no Health Check
        # Esperamos o serviço 'app' (acessível via localhost:5000) estar totalmente pronto.
        run: |
          echo "Aguardando o serviço 'app' (Flask) iniciar..."
          for i in {1..30}; do
            if curl -s http://localhost:5000/ > /dev/null; then
              echo "✅ Flask disponível!"
              break
            fi
            sleep 2
          done
          # Se o loop terminar sem sucesso, a pipeline irá falhar

      - name: Instalar jq para análise de JSON
        run: sudo apt-get update && sudo apt-get install -y jq

      - name: Criar pasta reports
        run: mkdir -p reports

      - name: Executar ZAP Baseline Scan (Comando Docker Direto)
        run: |
          # O ZAP acessa a aplicação pelo nome do serviço 'app' e sua porta interna
          TARGET="http://app:5000"
          REPORT_DIR="/zap/wrk"

          # Executa o ZAP, anexando-o à mesma rede dos serviços
          docker run --rm -v ${{ github.workspace }}/reports:${REPORT_DIR}:rw \
            --network ${{ job.services.app.network }} \
            ghcr.io/zaproxy/zaproxy:stable \
            zap-baseline.py \
            -t $TARGET \
            -r ${REPORT_DIR}/zap_report.html \
            -J ${REPORT_DIR}/zap_report.json \
            -l FAIL \
            -I # -I (Ignore) previne falhas se não houver alertas, mas ajuda a ignorar avisos menores.

      - name: Verificar vulnerabilidades críticas ZAP
        run: |
          # Instalar jq se ainda não estiver disponível no runner (já adicionado acima, mas redundância segura)
          CRITICAL=$(jq '.alerts[] | select(.risk=="High") | .name' reports/zap_report.json)
          if [ ! -z "$CRITICAL" ]; then
            echo "🚨 Vulnerabilidades críticas encontradas:"
            echo "$CRITICAL"
            exit 1
          else
            echo "✅ Nenhuma vulnerabilidade crítica encontrada."
          fi

      - name: Upload do Relatório DAST
        uses: actions/upload-artifact@v4
        with:
          name: Relatorio-DAST-ZAP
          path: reports/

  notification-on-failure:
    name: 3. Notificar Falha
    runs-on: ubuntu-latest
    if: failure()
    needs: [build-and-scan, dynamic-analysis]

    steps:
      - name: Enviar notificação para o Slack
        uses: rtCamp/action-slack-notify@v2
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK }}
          SLACK_TITLE: "🚨 Falha no Pipeline de Segurança!"
          SLACK_MESSAGE: "O pipeline no repositório ${{ github.repository }} falhou. Verifique os logs para mais detalhes."
          SLACK_COLOR: "danger"
          MSG_MINIMAL: true
